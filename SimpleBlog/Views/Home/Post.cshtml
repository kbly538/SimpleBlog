@model Post

@{ 

}

<h2>@Model.Title</h2>
@if (!String.IsNullOrEmpty(Model.Image)) {
    var imagePath = $"/image/{Model.Image}";
    <img src="@imagePath" alt="Post Image" />
}
<p>@Model.Content</p>